import React from "react";
import {Text, StyleSheet} from 'react-native';

const ComponentScreen = () => {
    return <Text>Component Screen!</Text>;
};

const style = StyleSheet.create({
    textStyle: {
        fontSize: 24,
    }
});

export default ComponentScreen;